# 본 파일을 .env로 복사 후 값 설정. .gitignore에 의해 .env는 추적되지 않음.

# 활성 모델: exaone | qwen
# - 국내 대회 운영: exaone
# - 개발/빠른 반복: qwen
LLM_MODE=qwen

# sglang OpenAI 호환 엔드포인트 (서버는 scripts/serve_*.sh로 별도 기동)
LLM_ENDPOINT=http://localhost:30000/v1
