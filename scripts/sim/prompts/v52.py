"""후보 v52 — 저녁 집 이벤트에서 reasoning 이 빠지는 것을 막는다. 한 줄을 고쳐 쓴다.

그리스디에서 v45 가 남긴 23칸 중 **explanation 7칸**의 정체를 원문에서 확인했다.
일곱 건 모두 같은 모양이다.

    intent 는 있고  reasoning 키 자체가 없다    (빈 문자열도 null 도 아니다)

빠지는 자리도 쏠려 있다. 하루의 끝머리, **집에서 보내는 저녁 이벤트**다.

    22:00  residence  집  intent="취침 준비"   reasoning 없음
    ...    그 앞뒤의 기상·식사·게임 이벤트에는 전부 들어 있다

본문은 이미 이렇게 적고 있다.

    **모든 이벤트는 time · reasoning · trigger 필드를 반드시 포함**. 절대 생략 금지.
    필드 이름은 정확히 `reasoning` 이다. ... 하루의 마지막 이벤트(귀가·취침)까지 똑같이 쓴다.

**"마지막 이벤트(귀가·취침)" 라고 적은 것이 좁다.** 빠진 것은 마지막 이벤트가
아니라 그 앞의 '취침 준비' 였다. 괄호가 예시가 아니라 목록으로 읽히면, 목록에
없는 저녁 이벤트는 면제로 읽힌다.

그래서 새 규칙을 더하지 않는다. **이미 있는 문장의 범위를 넓혀 쓴다.**
그리고 왜 빠지는지에 한 마디를 붙인다 — 늘 하는 일이라 쓸 말이 없다고 여기는
자리이므로, 늘 하는 일이라도 오늘 그렇게 한 이유를 적으라고 말한다.

7칸 중 5칸이 한 사람(10대 고등학생)이다. zone 결함과 같은 성질이다 —
무작위 잡음이 아니라 특정 문맥에서 매번 같은 자리가 빈다.

**형식은 건드리지 않는다.** 이벤트 JSON 예시 열넷, 스키마, anchor 규칙 전부 그대로다.
"""
from __future__ import annotations

from .p012 import format_dawn_blocks  # noqa: F401  (본문 공통)
from .v45 import SYSTEM_PROMPT as _V45

REWRITES = (
    # '마지막 이벤트(귀가·취침)' 이 목록으로 읽히면 '취침 준비' 가 면제가 된다.
    # 범위를 저녁 전체로 넓히고, 빠지는 이유("쓸 말이 없다")를 막는다.
    (
        '필드 이름은 정확히 `reasoning` 이다. `reason`·`reasonning` 은 무효다. '
        '하루의 마지막 이벤트(귀가·취침)까지 똑같이 쓴다.',
        '필드 이름은 정확히 `reasoning` 이다. `reason`·`reasonning` 은 무효다. '
        '저녁에 집에서 보내는 이벤트(귀가·집안일·정리·취침 준비·취침)에도 하나도 빠짐없이 쓴다 — '
        '늘 하는 일이라 쓸 말이 없어 보이는 자리일수록, 오늘 그 시각에 그렇게 한 이유를 적는다.',
    ),
)


def build() -> str:
    s = _V45
    for old, new in REWRITES:
        if old not in s:
            raise ValueError('v45 본문이 바뀌었다 — 바꿔 쓸 줄을 찾지 못했다: %.40s' % old)
        s = s.replace(old, new, 1)
    return s


SYSTEM_PROMPT = build()
