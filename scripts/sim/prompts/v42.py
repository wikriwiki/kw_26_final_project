"""후보 v42 — v40 에 원문이 실제로 틀린 자리만 고친다. 강조를 더하지 않는다.

v5 의 원문 계약 실패 64건을 응답 원문에서 열어 보면 다섯으로 모인다
(`experiments/WHAT_v5_ACTUALLY_GETS_WRONG.md`).

    31건  집·직장에서 먹은 끼니를 category='식사' 로 적는다
    24건  출퇴근을 zone 앵커 + category='직장' 으로 적는다
    23건  필드 이름을 'reasoning' 이 아니라 'reason' 으로 적는다
     3건  time 을 빈 문자열로 둔다
     3건  L1 자리에 하위 업종·동의어를 쓴다 ('약국' 2 · '레저' 1)

전부 형식이고, 전부 정책과 무관하다. 그리고 **강조가 부족해서 난 것이 아니다.**

①은 프롬프트가 이미 세 번 금지한다(31·43·49행). 그런데 열 줄 위에서 반대로 말한다 —
"식사 = 끼니" 라고 해 놓고 "집에서 먹은 끼니는 집" 이라고 한다. 모델은 그 모순을
자연스러운 쪽으로 푼다. 고칠 것은 강조가 아니라 **정의**다: 이 목록은 활동의 종류가
아니라 가게에서 돈을 쓰는 업종이다.

②는 어휘에 아예 없다. 출근은 집도 직장도 아니고 가게도 아니다. 빈자리를 모델이
메운다. 그래서 빈자리를 **말로 채운다** — 이동 자체는 이벤트가 아니라고.

③은 내용이 멀쩡히 들어 있고 철자만 틀렸다. 스물세 응답에서 하나씩, 전부 하루의
마지막 쪽 residence 이벤트에서 났다. zone 714개·workplace 263개에서는 한 번도 안 났다.

④ time 은 "반드시 포함" 목록에 없었다. 빈 문자열이 세 번 났다.
⑤ 어휘표는 열넷을 나열하지만 **그 밖의 이름을 만났을 때 어떻게 하라는 말이 없다.**
   '약국'·'레저' 가 L1 자리에 그대로 들어왔다.

**빈자리만 채우고 강조는 더하지 않는다.** 프롬프트가 이미 말하고 있는 것 —
"이벤트 간 최소 체류 20분", "위 'zone 후보' 목록의 코드만 그대로 복사할 것" — 은
건드리지 않는다. 세 번 말해도 서른한 번 어기는 것을 봤으므로 같은 말을 네 번째로
하지 않는다. 그 둘이 덮는 남은 실패(시간 간격 4건·목록 밖 코드 2건)는 **그대로 남긴다.**

저장된 원문으로 계산한 상한 — 고침이 완벽히 들었을 때의 값이고 예측이 아니다.

    v5             128/192   66.7%
    ①②③           183/192   95.3%
    ①②③④⑤         189/192   98.4%
    관문            183/192   95.0%

v10 이 예시를 없애 7.3% 로 무너진 것과 반대 방향이다 — 형식을 더 정확하게 만든다.
"""
from __future__ import annotations

from .p012 import format_dawn_blocks  # noqa: F401  (본문 공통)
from .v40 import SYSTEM_PROMPT as _V40

# (원문, 바꾼 글). 하나라도 못 찾으면 build 가 멈춘다.
REWRITES = (
    # ① 업종 목록이 무엇의 목록인지 먼저 말한다.
    (
        '[카테고리 어휘]',
        '[카테고리 어휘]\n'
        '이 목록은 활동의 종류가 아니라 **가게에서 돈을 쓰는 업종**이다. 집에서 차려 먹거나\n'
        "직장 구내에서 해결한 일은 가게 방문이 아니므로 category 는 '집'·'직장' 이다.",
    ),
    # ① 같은 모순을 정의 쪽에서도 없앤다.
    (
        '- **식사** = 식당·한식·양식·중식·일식·분식 등 끼니. '
        '음주 동반이라도 끼니가 주목적이면 식사.',
        '- **식사** = **식당에서 사 먹는** 끼니. 한식·양식·중식·일식·분식 등. '
        '음주 동반이라도 끼니가 주목적이면 식사.\n'
        "  집에서 차려 먹은 아침·저녁, 직장 구내식당·도시락은 category='집'·'직장' 이다 — "
        '가게에 가지 않았다.',
    ),
    # ② 어휘에 없는 경우를 명시한다.
    (
        '- 절대 금지: anchor=\'residence\' + category=\'편의점/식사/카페/한식/...\' 같은 조합. '
        '외출 카테고리면 무조건 zone.',
        '- 절대 금지: anchor=\'residence\' + category=\'편의점/식사/카페/한식/...\' 같은 조합. '
        '외출 카테고리면 무조건 zone.\n'
        '- **출근·퇴근 같은 이동 자체는 이벤트가 아니다.** 도착해서 하는 일을 적는다 — '
        "출근은 anchor='workplace' category='직장', 귀가는 anchor='residence' category='집'.\n"
        '  이동 중에 가게에 들렀다면 그 가게가 별도의 zone 이벤트다.',
    ),
    # ③ 필드 이름을 마지막 줄까지 못 박는다. ⑤ time 도 같이 — 빈 문자열이 세 번 났다.
    (
        '**모든 이벤트는 reasoning + trigger 필드를 반드시 포함**. 절대 생략 금지.',
        '**모든 이벤트는 time · reasoning · trigger 필드를 반드시 포함**. 절대 생략 금지.\n'
        '필드 이름은 정확히 `reasoning` 이다. `reason`·`reasonning` 은 무효다. '
        '하루의 마지막 이벤트(귀가·취침)까지 똑같이 쓴다.\n'
        'time 은 빈 문자열이 될 수 없다. 언제인지 모르겠으면 그 이벤트를 넣지 않는다.',
    ),
    # ④ 하위 업종·동의어를 L1 으로 올리는 규칙. 어휘표에 없던 빈자리다 —
    #   '약국' 2건, '레저' 1건이 L1 자리에 그대로 들어왔다.
    (
        "- 즉 **'술'은 행위, '주점'은 가게 카테고리** — 편의점·마트에서 술 사오는 건 주점이 아님.",
        "- 즉 **'술'은 행위, '주점'은 가게 카테고리** — 편의점·마트에서 술 사오는 건 주점이 아님.\n"
        "- category 자리에는 **위 열넷 중 하나만** 쓴다. 더 좁은 이름이 떠오르면 그것은 "
        "sub_category 이고, category 는 그것이 속한 L1 이다 — 약국·의원·병원은 건강, "
        "영화관·체육시설은 여가, 학원은 교육, 슈퍼마켓은 마트.",
    ),
)


def build() -> str:
    s = _V40
    for old, new in REWRITES:
        if old not in s:
            raise ValueError('v40 본문이 바뀌었다 — 바꿔 쓸 줄을 찾지 못했다: %.40s' % old)
        s = s.replace(old, new, 1)
    return s


SYSTEM_PROMPT = build()
