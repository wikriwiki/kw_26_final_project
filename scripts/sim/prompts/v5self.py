"""후보 6 (v5self) — 소비성향의 **척도를 사람에게 묶는다**.

사전등록: `experiments/plan_channel/prereg_v5self.md` (값 보기 전에 씀)

## 무엇을 겨냥하나

`daily_propensity` 가 **사람을 안 탄다**. 800칸에서 잰 값이다.

    원값 vs 그 사람의 중심값   r = -0.155     <- 사실상 0, 부호도 반대
    원값 고유값 17개 · 0.68 이 383칸(48%)
    클램프가 **위로 올린 칸 30%** · 아래로 깎은 칸 10%

모델이 누구에게나 0.68 을 적고, 클램프가 그 실패를 메우느라 30%를 끌어올린다.
그 메움이 **정책 반응까지 함께 누른다** — 원값 +5.72% 가 클램프 뒤 +3.55% 가 된다.

## 왜 척도인가

`_OPEN` 은 "금액이 아니라 오늘 소비하려는 **비율**" 이라고만 하고 **무엇에 대한
비율인지 말하지 않는다.** 고려하라고 든 것에도 '평소 자신의 씀씀이' 가 없다 —
어제 컨디션·개인 잔액·오늘의 조건·일정·실제 생활 필요뿐이다. 페르소나에
`spend_decile` 과 `income` 이 이미 들어 있는데 쓰라는 말이 없으니 최빈값을 적는다.

## 금지 규칙 대조

사용자가 못 박은 규칙: **정책 결과의 노골적인 방향이나 수치로 유도하지 말 것.**

덧대는 문장은 정책을 **언급하지 않는다.** 방향(올려라/내려라)도 수치도 없다.
v7·v9 가 제외된 이유는 채점 지표의 한쪽 방향을 금지한 것이었는데, 여기엔 방향이
없다. 이것은 **출력 척도의 정의**이고, 정의가 없어 모델이 못 쓰던 정보를 쓰게 한다.

v5 는 한 바이트도 고치지 않는다. 덧대기만 한다.
"""
from __future__ import annotations

from .candidates import make_system_prompt
from .p012 import format_dawn_blocks  # noqa: F401  (본문 공통)

# 척도를 사람에게 묶는 한 문장. 기준이 무엇인지만 말하고, 어느 쪽으로 가라고는
# 하지 않는다. "얼마나 벗어나는지" 는 양쪽으로 열려 있다.
_SELF_SCALE = """이 비율의 기준은 사람마다 다르다. 위에 주어진 본인의 소비수준과 소득이 그 기준이고,
오늘 값은 그 기준에서 얼마나 벗어나는지를 나타낸다."""

_BASE = make_system_prompt("v5")

# _OPEN 블록 끝(= 첫 빈 줄 앞)에 덧댄다. v5 의 문자열을 바꾸지 않고 이어 붙이기만
# 하므로, 아래 검증이 깨지면 v5 가 바뀐 것이고 그때는 후보를 다시 만들어야 한다.
_ANCHOR = "어제 컨디션, 개인 잔액, 오늘의 조건, 일정, 실제 생활 필요를 함께 고려한다."
if _ANCHOR not in _BASE:  # pragma: no cover - 구조가 바뀌면 조용히 지나가지 않는다
    raise RuntimeError(
        "v5 의 _OPEN 이 바뀌었다. v5self 가 어디에 덧대는지 다시 정해야 한다.")

SYSTEM_PROMPT = _BASE.replace(_ANCHOR, _ANCHOR + " " + _SELF_SCALE, 1)
