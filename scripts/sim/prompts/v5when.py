"""v5 + **적립형은 '돈이 생겨서' 가 아니라 '이번 달이라서' 다** — 후보 4.

## 무엇이 잘못돼 있나

이벤트를 만드는 대목이 통째로 **오늘 돈이 들어오는 정책**을 전제한다.

    p012.py:86  "다만 **쓸 수 있는 돈이 늘면** 하루 일정이 달라지기도 한다"
    p012.py:87  "옷·신발·가전처럼 **목돈이 있어야** 사게 되는 물건"
    p012.py:90  "**쓸 수 있는 목돈이 생겼을 때** 사람들이 흔히 하는 일은..."
    p012.py:92  "반대로 **목돈이 없으면** 그런 일은 다음으로 미루고 소액 지출로 버틴다"

지갑형(쿠폰·지원금)에는 맞는 서술이다. **적립형에는 오늘 들어오는 돈이 없다.**
그러면 이 문장들을 그대로 읽은 에이전트는 마지막 줄에 걸린다 —
"목돈이 없으면 다음으로 미루고 소액으로 버틴다".

**제도의 기전과 반대 방향이다.** 적립형은 돈을 주는 것이 아니라 **언제 쓰느냐**를
바꾸는 제도다. 이번 달에 한 지출은 실적에 들어가고 다음 달에 한 지출은 안
들어간다. 같은 돈을 같은 데 쓰되 **시점**이 달라지는 것이 이 제도의 전부다.

## 덧대는 것 — 방향이 아니라 **빠진 채널**

"더 쓰라" 고 하지 않는다. 목돈 채널 말고 **시점 채널**이 있다는 사실을 적고,
그 채널이 이 사람에게 해당하는지는 기존 문장들이 정하게 둔다. 마지막 문장이
**반응하지 않는 경우를 명시**한다 — 당길 일이 없으면 그대로다.

감쇠 두 줄("기계적으로 더하거나 빼지 않는다"·"기계적으로 올리거나 내리지는
않는다")은 **그대로 둔다.**
"""
from __future__ import annotations

from .p012 import format_dawn_blocks  # noqa: F401  (본문 공통)
from .v5 import SYSTEM_PROMPT as _V5

_ANCHOR = ("일을 처리하는 것이다. 반대로 목돈이 없으면 그런 일은 다음으로 미루고 "
           "소액 지출로 버틴다.")

WHEN_LINE = (
    "\n  **다만 목돈이 생기는 것만이 미뤄둔 일을 앞당기는 이유는 아니다.** "
    "적립(캐시백)형 제도에서는 오늘 들어오는 돈이 없다 — 대신 **이번 달에 한 "
    "지출만 실적에 들어가고 다음 달로 미루면 안 들어간다.** 그래서 어차피 "
    "언젠가 할 일이라면 이번 달 안이 그 일을 하기 좋은 때가 되기도 한다. "
    "돈이 늘어난 것이 아니라 **때가 달라진 것**이다. "
    "물론 당겨올 일이 애초에 없거나 지금 형편이 안 되면 그대로다."
)


def _build() -> str:
    s = _V5
    if _ANCHOR not in s:
        raise RuntimeError("v5 본문에서 목돈 문장을 못 찾았다 — 본문이 바뀌었다")
    return s.replace(_ANCHOR, _ANCHOR + WHEN_LINE, 1)


SYSTEM_PROMPT = _build()
