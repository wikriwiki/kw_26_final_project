"""DASOL report orchestration adapters.

The calculation engine remains in ``scripts/sim``.  This package only exposes
the structured catalog and the safe, fixed-argument CLI used by the web API.
"""
