# Neo4j Day 0 적재 의존성
neo4j>=5.20,<7
openpyxl>=3.1
pyyaml>=6.0

# (선택) V-WORLD geocoding for 03a/03b
requests>=2.31

# Policy pipeline (scripts/policy_pipeline/)
pydantic>=2.7,<3

# Policy pipeline — LLM 경로 (자연어 정책 추출 시 필요)
openai>=1.54,<2

# Policy pipeline — Watchdog 워커 실행 시 필요
watchdog>=4.0,<7

# Persona — NVIDIA 실데이터 다운로드 (scripts/persona/prepare_nvidia.py) 시에만 필요
# 페르소나 생성 자체(build_*)는 datasets 불필요. 동봉 fixture/full 파일만 읽음.
datasets>=2.14
