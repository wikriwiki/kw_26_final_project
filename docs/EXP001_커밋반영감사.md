# EXP-001 커밋 반영 감사 — 지난 한 달 커밋이 시뮬에 제대로 반영됐는가

> 목적: 지난 30일 26개 커밋의 변경이 EXP-001 시뮬(2025-07-14~27 백테스트)에 실제로 반영되고
> 결과에서 검증되는지 확인.
> **런이 사용한 코드 = main HEAD `356b70c`** (그 이후 커밋 3개 `7ddd402·0987a6e·3b22b97`는
> 런 종료 후 작성한 문서라 시뮬 행동에 무영향). 즉 **런은 `356b70c` 및 그 이전 모든 시뮬 커밋을 포함**한다.

범례: ✅ 반영·결과로 검증됨 · 🔶 로직 반영, 세부 검증은 소실 그래프 필요 · ⚪ 인프라·운영·문서(행동 무관)

---

## A. 정책·쿠폰·표본 핵심 — 반영 및 검증 완료 ✅

| 커밋 | 변경 내용 | 반영 | 결과에서의 검증 증거 |
|---|---|---|---|
| `ed4c3f4` | **실제 정책일·10분위 차등지급·모집단 비례 표본** | ✅ | 정책 시행 **2025-07-21** 정확 · 분위별 지급 **1분위 40만·2분위 30만·3~10분위 15만** 전 분위 정합 · 표본 **25/25 자치구, 분위 편차 ±0.01%p** |
| `00ce723` | **서울 전역 정책 applied_to 배선** + preflight DB 게이트 | ✅ | applied_to **25개 자치구** 연결 · 정책 노출 **14,621명** (이 수정 없으면 노출 0 → 전 기간 baseline이 될 뻔) |
| `9be31f1` | 쿠폰 **사용처 판정**(POI별 쿠폰 가능 여부 + 사용 유도·차단) | ✅ | **지원금 귀속 소비 100% 가맹점**(비가맹 0건) — 사용처 하드 차단 정상 작동 |
| `cd3245b` | 쿠폰 백필에 **실측 가맹점 조인**(서울사랑상품권) | ✅ | 실측 가맹점 인덱스 **137,133건**, commerce POI 537,489개 판정(실측 매칭 15.9%) |
| `35072f0` | 정책 주입 **프롬프트 상세화** + 상품권 가맹점 실측 매핑 | ✅ | 정책 카드 렌더 정상(preflight 확인), 쿠폰 마커·정렬 가점 반영 |
| `e76d255` | v5 **WORKS_AT 시드** + 안정화 | ✅ | 직장 방문 **평일 37~41% vs 주말 0.7%** — 직장 외출 재현(과거 v4 0% 버그 해소) |
| `8a6cab8` | **memory 시간감쇠 부호 수정** + 정책 지원금 프롬프트 개선 | ✅ | 방문메모리 최신 참조(avg 07-16 0 → 07-27 3.4로 증가, 오래된 기억 증폭 없음) · 지원금 실제 사용(소진율 99.9%) |
| `c259cbd` | **EXAONE thinking 강제 끔**(enable_thinking=False) | ✅ | tokens_out **~1,500**(thinking ON이면 15,000 폭주) · err율 **0.004%**(JSON 파싱 정상) |

**→ 이 실험의 정책 인과 배선(누가·얼마·어디서 지급받고 어디서만 쓸 수 있나)과 핵심 행동 로직은 전부 반영·검증됨.**

---

## B. 소비 세부 로직 — 반영됨, 세부 검증은 소실 그래프 필요 🔶

| 커밋 | 변경 내용 | 반영 | 상태 |
|---|---|---|---|
| `907e097` | **POI 가격대 도입** — 선택 가게에 따라 소비액 달라짐 (`poi_price.py`, `consumption.py`) | 🔶 | 소비 분산은 확인(중앙 88,751·p90 240,817·최대 906k) → 가게별 가격 반영 흔적. 단 **"어느 가게에 얼마" 방문별 단가는 정책주간(07-21~27) 그래프 소실**로 직접 검증 불가 |
| `98d32c2` | 소비 알고리즘 **가격 축을 실측 데이터 계층으로 재구현** (`poi_price.py` v2, 카카오 메뉴가) | 🔶 | 위와 동일 — 로직은 런에 포함, 방문별 실측단가 검증은 재실행 필요 |
| `3c046f2` | **제한 예산 봉투 + windfall 감쇠** + preflight | 🔶 | windfall("공돈") 프레이밍이 소진 궤적에 강하게 반영(당일 69.7% 과속 소진 — [[EXP001_CAVEATS]]) · preflight ✅ 실행 · 예산 봉투 클램프 세부는 부분 검증 |
| `953bee2` | **리뷰 노출·사고변화 기록**(추가 LLM 호출 0) | 🔶 | `review_lookup_count` metrics에 존재하나 대부분 0(리뷰 데이터 희소) — 반영됐으나 영향 미미 |

**→ 소비 "금액이 가게·가격대에 따라 달라지는" 로직은 코드에 반영됐고 소비 분산으로 간접 확인되나, "방문별 어느 업종·가게" 세부는 정책주간 그래프 소실로 직접 검증 불가. 재실행 시 검증 가능.**

---

## C. 인프라·운영·문서 — 시뮬 행동 무관 ⚪

| 커밋 | 성격 |
|---|---|
| `f9e9078` `6bfffd1` | EXP-001 GPU LIVE 셋업 스크립트(A100·양자화 폴백·런북) — 실행 환경 |
| `516a840` | Cypher deprecation 경고 제거(동작 동일) |
| `356b70c` | Neo4j 알림 억제(로그 정리, 미세 성능) |
| `6280340` | GitHub Actions CI/CD |
| `320386b` `233301d` `e0d4814` | 브랜치 병합 |
| `43f1374` `df9dad9` `17513fd` `7ddd402` `0987a6e` `3b22b97` | 문서·설계서 |

---

## 종합 판정

- **정책·쿠폰·표본·직장외출·메모리·thinking = 8개 핵심 커밋 전부 반영·결과로 검증됨.** 이 실험의 정책 효과 배선은 신뢰 가능.
- **소비 세부(POI 가격대·카테고리) = 로직은 반영됐으나 방문별 세부 출력이 정책주간 그래프 소실로 직접 검증 불가.** 소비 분산·총액으로 간접 확인. **재실행하면 이 부분까지 검증 완결.**
- **미반영 커밋 없음.** 런이 `356b70c` 기준이므로 그 이전 모든 시뮬 커밋이 포함됨. 누락된 시뮬 로직 커밋은 없다.

**결론**: 지난 한 달의 시뮬 관련 커밋은 **빠짐없이 런에 반영**됐고, 그중 정책·쿠폰·표본·직장외출·메모리 등 **핵심은 결과 데이터로 직접 검증**된다. 소비 단가·카테고리 세부만 인프라 장애로 인한 그래프 소실 탓에 직접 검증이 보류되며, 이는 재실행으로 해소된다.
