# S4 Independent Critic — round 1

검토 입력: 기준서 S4 완료 기준, S4 계약, `MonitorPage.tsx`, S2 runner/API,
S1 rescue 픽스처, typecheck/build 및 API tests

## 기준 대조

통과:

- B1: 진행·오류·실패율·ETA는 run/days/log_hint/metrics 응답에서 나오며 하드코딩 KPI가 없다.
- B4: 일자 목록은 status_scan 경로를 사용하고 상세 aggregate는 lazy-load한다. SSE interval 기본값은 2초다.
- B6: 선택 run과 policy는 localStorage로 복원하고, 서버 상태는 새로고침 시 다시 읽는다.
- B8: UI 비활성화 외에 S2 원자 lock/409가 실행을 차단하고, lock owner/start/PID를 표시한다.
- rescue: 계획·진행률·실패 checkpoint·병목 하위 경로 미확인을 각각 `UnknownTag`/fallback으로 표시한다.
- 병목: 완전 timing 리포트와 metrics phase fallback을 서로 다른 badge와 source로 구분한다.
- 실행 중 안전: 프런트는 2초 SSE와 5초 lock 확인만 사용하며 원본 JSONL을 브라우저로 적재하지 않는다.
- 반응형 workspace는 900px 이하 1열로 내려가 768px에서 가로 스크롤을 만들지 않는다.

## 기준 위반

없음.

## 판정

**통과 — S4 게이트 통과.** 실행 명령 미구성은 서버가 503으로 공개하는
안전한 운영 설정 상태이며, 화면이 성공으로 오판하지 않는다.

