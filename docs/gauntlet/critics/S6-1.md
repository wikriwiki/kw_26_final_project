# S6 Independent Critic — round 1

검토 입력: 기준서 §2.1, S6 계약, `web/ui/src/styles/`, shell/nav/routes,
typecheck/build 결과

## 기준 대조

통과:

- 운영 화면 우선: compact page head와 run/policy/status context가 있고 hero/마케팅 카피가 없다.
- 고정 workspace: 224px rail, 48px topbar, primary/inspector 그리드가 기준 범위 안이다.
- 내비게이션: 정책 설정·실행 모니터·결과 시각화·데이터 계약·시스템 5개가 실제 route에 연결된다.
- 객체/상태: 정책·run·day·lock·artifact·contract를 object label과 source line으로 표시한다.
- 색: 토큰이 muted 의미색이고 선택/진행/경고/실패를 구분한다. 장식용 gradient/AI purple/sparkle UI가 없다.
- 기하: 스타일 파일에서 `gradient`, `backdrop-filter`, `box-shadow`, `Plus Jakarta Sans`, `Poppins` 0건; 숫자 radius >4px 0건.
- 반응형: 900px 이하 rail→tab, workspace→1열. table/iframe은 자체 컨테이너에서 overflow를 처리한다.
- 상태 키트: system route에서 loading/empty/error/partial state 실물이 확인된다.

## 기준 위반

없음.

## 판정

**통과 — S6 게이트 통과.** S3~S5 화면은 공통 workspace와 상태 토큰을
공유한다.

