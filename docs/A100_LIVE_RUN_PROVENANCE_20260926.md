# A100 진행 런의 실행 코드 보존 (2026-09-26)

이 문서는 현재 P012 28일 **진단 런**의 재현 근거와 다음 실험으로 전환할 때의 경계다. 월 전체의 정책 효과 크기 또는 범용 프롬프트의 정확도 검증 결과로 사용하지 않는다.

## 실행 상태와 코드 사본

- 2026-09-26 04:56 KST에 PID `1474197`이 `/data/repo`에서 `run_simulation.py --start 2021-10-01 --days 28 --limit 500 --workers 48 --environment covid_2021`로 실행 중임을 확인했다. 출력 위치는 `/data/p012_28d/28d_v5`, 프롬프트 변형은 `v5`, 모델 API는 로컬 SGLang이다.
- 서버 Git HEAD는 `651d6094369fd21976756f54e604da3da7d2af11`이다. 당시 `git status --porcelain`에는 추적 수정과 미추적 파일을 합쳐 66개 경로가 있었다. 현 로컬 브랜치 HEAD는 `000af11`이므로 로컬 코드만으로 이 진행 런을 설명하지 않는다.
- 실행 코드를 서버의 `/data/codex_takeover_20260926/a100_live_source_20260926.tar.gz`에서 로컬 `output/recovery_20260926/a100_live_source_20260926.tar.gz`로 복사했다. SHA256은 양쪽 모두 `450b5780ca222e2e345c093fc4db7f32d0a772bbb9c993f84d74033e868e770c`, 크기는 1,907,059바이트다. 함께 복사한 `a100_live_source_20260926.manifest.json`에는 실행 명령, 안전한 환경변수, 서버 HEAD, 변경 경로와 아카이브 해시가 있다.
- 아카이브는 `scripts/sim`, `scripts/neo4j_load`, `scripts/report`, 코로나 환경 자료, 채점표, 정책 JSON을 담는다. 실행 스크립트 `/data/run_p012_28d.sh`에는 인증 정보가 있어 넣지 않았다. 아카이브는 **실행 코드 증거**이며 Neo4j DB 덤프, Python 패키지 잠금 파일, 프로세스 메모리 또는 전체 재시작 체크포인트가 아니다. 시작 시 Python이 로드한 내용과 캡처 시 디스크 내용이 완전히 같은지는 단정할 수 없다.
- 아카이브와 현재 로컬 파일의 원시 바이트를 대조하면 `scripts/sim/prompts/v5.py`와 `v51.py`, `distancing_schedule.json`은 같다. 반면 `run_simulation.py`, `consumption.py`, `stage2_poi.py`, 프롬프트 선택기 `__init__.py`, `covid_2021.py`, `P012.json`은 다르다. 따라서 같은 `v5` 파일이라는 사실만으로 두 실행 환경을 동일하다고 볼 수 없다.
- 현재 런의 완료 날짜 원장은 별도 일별 아카이브로 보존한다. 10월 17일까지 서버 밖 복사와 해시 검증이 완료됐다. 날짜별 파일과 그래프 기반 계측은 서로 대체할 수 없다.
- 현재까지의 코드·완료일 아카이브와 매니페스트 10개 파일(합계 10,823,365바이트)을 `C:\Users\Administrator\Documents\kw26_a100_recovery_20260926`에도 복제했고, 작업 폴더의 각 파일과 SHA256이 모두 일치한다. 이후 완료일은 별도로 복제·검증해야 한다.

## 전환 관문

1. 현재 프로세스와 그래프를 수정하거나 `/data/repo`에서 `git pull`하지 않는다. 날짜가 완성될 때마다 일별 metrics·Agent·State·INCLUDES·Policy를 서버 밖에 복사하고 해시를 확인한다.
2. 런의 최종 `summary.json` 및 PID 종료를 확인한 다음 마지막 날짜까지 출력 완전성·Stage2 실패·정책 전달·원장 회계를 감사한다. `v5`의 정책별 효과 크기 적중으로 승격하지 않는다.
3. 새 실험을 위해 그래프를 reset하기 전에, 다른 사용 작업이 없는지 확인하고 Neo4j Community의 DB를 정지 상태에서 덤프해 서버 밖으로 복사·해시 검증한다. 일별 JSON 아카이브만으로 그래프를 복원할 수 있다고 가정하지 않는다.
4. 새 코드는 현재 더러운 체크아웃에 덮어쓰지 않는다. 깨끗한 별도 작업 디렉터리를 만들고, 서버 스냅샷과 로컬 변경을 대조한 뒤 기술 스모크 테스트 및 프롬프트·정책·환경 지문을 고정한다. 정책 팔에서는 **실제 적재한 정책 사본**으로 `python scripts/sim/policy_preflight.py --require-db <정책 사본>`을 실행해 Neo4j 노출 경로를 확인한다. 무정책 팔에서는 `python scripts/sim/policy_preflight.py --expect-no-policy`로 Policy 노드·적용 연결이 모두 0개인지 확인한다. DB 조회를 건너뛴 정적 점검 결과를 런 시작 허가로 쓰지 않는다. 이후 사전등록한 두 팔을 각각 독립 출력·그래프로 실행한다.

이 보존 조치로 SSH가 끊겨도 이미 내려받은 완료 날짜와 코드 사본은 남는다. 서버 장애 또는 회수 후 현재 실행을 중단 지점부터 완전히 복구할 수 있다는 보장은 아직 없다.

사전점검 관문을 현재 A100에 **읽기 전용**으로 시험했을 때 무정책 팔 검사는 `Policy 1개, applied_to 25개`를 탐지해 실패 판정을 냈다. 현재 P012 정책 팔에서는 예상된 결과다. 새 무정책 팔의 정책 0개를 증명한 것은 아니다.
