# Claude → Codex 재인수 기록 (2026-09-25)

## 사용자 목표와 복구한 대화

사용자 재확인: **시뮬레이션 출력으로 각 정책의 검증지표를 평가·비교할 수 있게 만든 후, 그 평가 체계로 범용 최적 프롬프트를 만든다.** 방향뿐 아니라 대략적인 효과 크기도 평가한다. 범용 프롬프트에 특정 정책의 정답 방향·실측 결과 수치를 넣지 않는다.

프로젝트 Claude 세션 `725cd283-3254-45a4-a7c1-b75adb99426b.jsonl`의 최근 대화와 도구 기록을 확인했다. 마지막 유효 작업은 9월 25일 13시대다. 사용자는 핵심 지표 한 개만이 아니라 **검증 가능한 모든 지표**를 실측과 면밀히 비교하도록 요청했다. 마지막 커밋은 `3cddb90`이며 P010 보관 원장 집계를 추가했다. 다음 작업으로 지표의 단위·기간 정합을 남겼다.

`experiments/SELECTED_PROMPT.md`, `FINAL_PROMPT_BUNDLE.md`, `NEXT_STEPS.md`도 확인했다. v5는 지금까지 시험한 후보 중 유지된 기준 프롬프트다. **최적성이 증명됐거나 프롬프트 개선 가능성이 소진됐다는 뜻으로 확대하지 않는다.** 해당 문서의 옛 진행률·종료 예상 시각은 아래 실시간 관측으로 대체한다.

## A100 실제 관측 (9월 25일 23:47~23:52 KST)

| 항목 | 확인한 값 |
|---|---|
| 제공 환경 | 메가존클라우드, NVIDIA A100-SXM4-80GB |
| 모델 | `LGAI-EXAONE/EXAONE-4.5-33B-AWQ`, SGLang |
| 실행 | P012, v5, 500명, 2021-10-01부터 28일, workers=48 |
| 프로세스 | runner 1474100 / simulation 1474197 |
| 시작 | 2026-09-25 11:33 KST 재실행 |
| 산출물 | `/data/p012_28d/28d_v5` |
| 진행 | 13일 완료; 14일째 452/500행 기록 중 (23:52 관측) |
| 완료분 상태 | 6,500 에이전트-일 중 ok 6,498 / error 2; 고유 시민 수와 구별 |
| 잔고 | 완료분의 정상 기록에서 잔고 측정 6,498건, 0 이하 0건 |
| 재시도 기록 | stage1 실패 시도 110건: ValidationError 73, JSONDecodeError 34, ValueError 3. 최종 실패 시민 수가 아님 |
| 정책 전달 | Neo4j에 P012 cashback 존재, 적용 시작 10-15, applied_to 25개 지역 |
| 자원 | 관측 시 GPU 사용률 100%, 메모리 약 78.9/81.9GB; /data 여유 850GB |

정책 적용 전 기간을 실행하고 있다. 정책 노드 존재는 확인했지만 **정책 적용 후의 실제 전달·행동 효과는 아직 확인하지 않았다.** 원장 증가로 실행 중임을 확인했다. 기존 로그는 필터링돼 일별 진행 출력이 보이지 않으므로 로그 침묵만으로 정지를 판단하지 않는다.

현재 프로세스 환경에서 직접 확인한 설정:

```text
SIM_PROMPT_VARIANT=v5
EXP_DAILY_INCOME=anchor
EXP_BALANCE_DAYS=39
EXP_SANGSAENG_BASE_RATIO=0.268
EXP_SEED_SANGSAENG=0
EXP_PLAN_DRIVES_TOTAL=0
EXP_POLICY_ANONYMOUS=1
EXP_DURABLES=1
EXP_CATLINE=fold
```

이 실행은 FINAL_PROMPT_BUNDLE의 권장 회계 수정(PLAN_DRIVES_TOTAL=1)과 다르다. 따라서 해당 수정의 효과를 검증한 런으로 표시하면 안 된다.

이전 실행은 정책 적재 후 reset으로 정책이 제거되는 문제를 겪었다. 재실행 로그에는 reset → 적재 순서와 적용 지역 확인이 있다. 로그에 남은 이전 실행의 DONE/결제 데이터 없음 메시지를 현재 실행 결과로 읽지 않는다.

## 재현성과 보존

로컬 HEAD는 `3cddb90`, 서버 `/data/repo` HEAD는 `651d609`이며 서버에 추적 파일 수정도 여러 개 있다. `consumption.py`, `score_policy.py`는 실제 내용이 서로 다르다. 실행 중인 서버를 pull/동기화하지 않았다. 서버 코드 차이를 검토하기 전 로컬 코드 기준으로 이번 실행을 설명하지 않는다.

v5 파일의 **원시 바이트 SHA256**는 서버·로컬 모두 `191ce28f3512280238749a9c130124896d4b7306f201b91bd2508597868222bb`다. 기존 문서의 `250311b63adbc4f6`과는 다르므로 이전 시험과 동일성을 문서의 해시만으로 단정하지 않는다. 현재 디스크 사본이 시작 시점에 로드된 코드와 동일한지는 별도 한계다.

완료된 10-01~10-13 원장 및 서버 코드·정책·환경 JSON 166개 파일을 보존했다:

- 서버: `/data/codex_takeover_20260925/checkpoint_2354.tar.gz`
- 로컬: `output/recovery_20260925/checkpoint_2354.tar.gz`
- 파일별 해시: 같은 디렉터리의 `manifest_2354.json`
- 아카이브 SHA256 양쪽 일치: `b3dd672b34cdda3f974b1468dd3b6417236659170f8df5c8425d0f9bfc9f4d1f`

이 사본은 **완전한 Neo4j 백업·재시작 체크포인트가 아니다.** SSH 연결과 별개로 nohup 프로세스가 실행 중이지만 서버 장애·회수 시 진행분 전체 복구를 보장하지 않는다. 그래프에서만 계산되는 항목은 완료 직후 별도 추출해야 한다. 다음 정책 reset 전에 원장뿐 아니라 per-agent 채점 입력 및 결과를 보존할 것.

## 비교표에서 이번에 바로 고친 문제

`scripts/report/all_indicators_table.py`를 수정했다. 원본 원장·채점표·프롬프트·서버 실행은 변경하지 않았다.

1. P012-3은 실측 크기가 없는 방향 지표다. 단위 불일치로 분류하지 않는다.
2. P012-4는 `fetch_cashback()`상 **종료일 State 누적액에서 계산한 캐시백 추정액**이다. 기존 '창 하루 평균' 설명이 틀렸다. 실측의 수급자/전체 분모와 관측기간 확인이 필요하다.
3. P012-6은 0~1 비율을 %로 환산해 표시한다. 이 변환만으로 월말 범위·모집단 차이가 해결되지는 않는다. 28일 관측은 31일 월 전체와도 다르다.
4. PT-1/2는 실측 크기와의 오차가 아니라 무효과의 동등성 검증 대상이다. 기존 등록 판정과 CI를 JSON에 유지한다. 관측값을 0으로 바꾸지 않는다.
5. P010-1의 0.216은 기존 자기보고 신규소비 비율로 남긴다. 무차원·숫자 근접만으로 검증 성공을 선언하지 않는다. BOK도 설문 기반 MPC라는 기존 자료가 있으므로 '자기보고라서 원천 비교 불가'라고 단정하지도 않는다. 조사 정의·기간·모집단·원장 계보를 확인해야 한다. 기존 CI는 시민-일을 독립 재표집하므로 독립 시민 수준 CI로 해석할 수 없다.
6. 기준선 블록의 note가 정책 결과로 선택되는 경로와, 순위 간격 0이 결측 처리되는 버그를 수정했다.

JSON 비교표에 metric, CI, 기존 등록 hit, 선택한 결과 블록과 설명을 함께 남긴다. 단위만 맞춘 기존 '대조가능' 4개도 아직 모집단·추정량·기간을 모두 검증했다는 뜻은 아니다. 이질적 지표의 %p 오차 합을 보편적 정확도 점수로 쓰지 않는다.

산출물: `output/recovery_20260925/indicator_comparison.json`, `docs/INDICATOR_COMPARISON_20260925.md`.
검증: `py -3 -m pytest tests/unit/report/test_all_indicators_table.py -q` → **21 passed**.

## 이어서 할 순서

1. 38개 지표 각각에 원문 근거, 추정 대상, 단위, 분자·분모, 대상 집단, 기간, 대조 설계, 출력 필드와 결측 규칙을 명시한다. 단위 환산과 추정 대상 변경을 구별한다. 새 기준은 버전을 붙이고 기존 등록 시험을 덮어쓰지 않는다.
2. P012 진행 런이 끝나면 정책 전달·원장 누락·지갑 상태 관문부터 확인하고, 그래프가 바뀌기 전에 채점 입력을 보존한다. 이후 기존 OFF 10-13:14 / ON 10-27:28 결과와 사전 드리프트를 구분해 보고한다. 월말 크기 검증의 한계도 함께 적는다.
3. P010 보관 원장의 출처·자기보고 산식·시민별 반복 관측을 점검한다. `score_policy.py`에서 `mpc_amount`가 총지출 차이와 같은 경로를 타는 점도 별도 해결 대상이다. 이름만 같은 서로 다른 추정량을 합치지 않는다.
4. 새 실험은 누락 지표를 채울 수 있는지와 검출력부터 확인한다. P015 재실험 금지, P010 원본 불변 조건을 유지한다. GPU를 추가 사용하기 전 현재 런·그래프 충돌 여부를 확인한다.
5. 평가 정의를 고정한 후 v5를 기준으로 범용 후보를 비교한다. 프롬프트 선택에 이미 사용한 정책은 개발 자료로 표시하고, 독립 검증에는 선택에 사용하지 않은 정책/시기 자료를 확보한다. 반복 시드·정책 간 일관성·방향·크기·위약을 함께 평가한다. 실측 결과를 프롬프트로 역주입하지 않는다.

기존 사용자 수정 `output/report/convergence.html`은 보존했다. 해당 화면은 이번 비교표 수정과 동기화하지 않았으므로 이번 인수 기록/JSON과 구분해서 읽는다.

## 9월 26일 00:51 KST 후속

동일 프로세스 1474197이 계속 실행되고, 원장은 10-14까지 완료 후 10-15의 381/500행까지 증가했다. 10-15 정상 행 381건에는 정책 노출 기록이 있다. 이 시점에 `grant_applied_today`와 `policy_spend_today`가 0인 것은 P012 캐시백의 월말 지급 기전과 구별해야 하며, 정책 작동 성공을 아직 입증하지 않는다.

P010 보관 원장의 `n=1971`은 **200명의 반복 시민-일 1,971개**였다. `score_p010_from_archive.py`를 시민(`aid`) 단위 재표집으로 수정하고, 기존 셀 단위 CI를 채점표의 `ci_cell_bootstrap_superseded`에 보존했다. 새 95% 구간은 **[0.1983, 0.2339]**, 가중 점추정치는 0.21598, 시민 수 n=200이다. 반복 관측의 독립성 오인을 고쳤으나 자기보고 비율과 BOK 조사 추정량의 일치 여부는 여전히 별도 감사 대상이다. 단위 시험 23개 통과.

## 9월 26일 00:55 KST — 표준 채점기의 P010 산식 수정

정적 코드 경로를 전수 점검하면서 `score_policy.py`가 `mpc_amount`를 `total_spend_paired`와 같은 함수에 연결한 것을 확인했다. 이는 원 단위 총지출 쌍체차를 비율 MPC로 부르는 오류다. MPC는 이제 `--metrics-dir`로 명시한 일일 원장에서 **정책 ON 날짜에 한해** 읽고, 정책결제액 가중 및 시민별 재표집 구간을 출력한다. 원장이 없거나 관측일이 빠졌거나 정책결제액이 없으면 채점이 실패하며, 총지출 쌍체차로 대신 채우지 않는다.

완료된 EXP-001 보관 원장 전체를 새 표준 경로로 재계산한 결과, 별도 보관 원장 채점기와 점추정치 `0.21598342818050267`, 시민별 CI `[0.1983349927789799, 0.2339214199811742]`, 시민 200명·시민-일 1,971개가 일치했다. `audit_indicator_coverage.py`는 이 경로에 `--metrics-dir`이 필요함을 알리고, 정적 계산 가능성과 실측 비교 가능성을 명확히 구별한다. 관련 시험 **26개 통과**. 실행 중인 A100 P012 코드는 변경하지 않았다.

## 9월 26일 — P012 원문 지표 재감사

KDI 원문 PDF 표 3-1·4-7을 시각 확인했다. 기존 P012-1 `+20.82%`는 실제로 **0.2082 log-point 삼중차분 계수**다. 시민의 짧은 OFF/ON 변화율 `+1.90%`와 직접 차감한 `18.92%p`는 취소했다. P012-2·5도 같은 로그계수 계열이다. 보고서의 47,880원과 21.0%는 **10~11월 수령자 기준**이며 월별 표는 10월 평균 약 47,828원·수령자 중 한도 도달 약 20.87%를 준다. 표본·기간을 맞추기 전에는 원문 크기와 차감하지 않는다.

표준 채점기의 P012-4·6 분모를 수령자로 고쳤고, 월말 관측과 정책 월 초 시행이 모두 확인되지 않으면 등록 적중을 내지 않도록 했다. 현재 A100 런(정책 10-15~ / 관측 10-28)은 **내부 진단 런**이며 10월 전체 실측 크기 검증 런이 아니다. 자세한 추정량·원문 위치는 `docs/P012_EMPIRICAL_ALIGNMENT_20260926.md`에 있다. 비교표의 잠정 %p 오차 합은 P012-1을 제외해 29.52→10.60이 되었으며, 이는 **측정 정의 정정**으로 인한 변화이지 성능 향상이 아니다.

## 9월 26일 — 모든 기존 크기 비교의 감사 결과

EM-3 `+7.3%`는 인용된 KDI PDF 8쪽 전체에서 확인되지 않았다. DS-1 `−14.1%`는 한식의 전년 대비 매출, DS-2 `+4.2%`는 팬데믹 시기 소매 평균 매출인데 시뮬은 11월 이틀 전후 변화를 썼다. 이 세 개도 기존 직접 오차에서 제외했다. **현재 38개 중 외부 실측 크기와 같은 추정량으로 확인된 비교는 0개**다. 어떤 프롬프트의 크기 유효성도 아직 입증되지 않았다. 정책별 계측·실험 재설계는 `docs/VALIDATION_GAPS_20260926.md`에 정리했다.

완료된 10-01~10-15 데이터에 대해 추가 원격 백업을 수행했다. `output/recovery_20260926/p012_completed_oct01_15.tar.gz`는 일별 metrics 15개, Neo4j에서 읽은 State 7,498행·결제 85,532행·Policy 1개를 담고 있다. 서버와 로컬 SHA256이 `defd15e418a72238db4a18057f539e65cbda118f6f80174f8566adb2c782a982`로 일치한다. `p012_completed_oct01_15.manifest.json`에 범위와 건수가 있다. **읽기 가능한 근거 사본이지 Neo4j 전체 복원용 덤프는 아니다.**

다음 달력 월 실험을 위해 `plan_writer.py`의 월별 총지출·적립업종 누적을 월 1일에 초기화하고 `dawn_context.py`가 그날 아침 같은 값을 보도록 맞췄다. 이 **로컬 변경은 실행 중인 A100 코드에 배포하지 않았다**. 후보 Cypher는 현재 Neo4j에서 읽기 전용 `EXPLAIN`으로 구문 검증했다. `score_policy.py`는 월말·월초 시행일에 더해 시민별 그 달 전체 날짜 State가 있는지 확인한다. 두 팔과 보존 관문은 `docs/P012_FULL_MONTH_PREREG_20260926.md`에 결과 확인 전에 등록했다.

## 9월 26일 — P010 MPC 영수증 귀속 감사

한국은행 참고 3은 전국 신청자 1,536명의 8월 13~20일 조사에서 사용·사용계획을 아홉 품목별 ‘쿠폰 없이도 살 것인가’로 구분했다. 보관된 시뮬 MPC 0.215983은 서울 합성 시민 200명의 거래별 `extra_spent`를 계획 결제액으로 가중해 만든 일별 비율을 다시 최종 결제액으로 가중한 값이다. 양의 결제 1,940칸 중 34칸에는 비율이 없고, 금액 기준 관측률은 98.52%다. 숫자가 0.21에 가까운 것을 외부 크기 검증으로 선언하지 않는다.

특히 **0.215983은 28일 전체 평균**이다. 등록된 ON 창 7월 22~23일로 제한하면 `0.120923`(시민 108명, 결제 귀속률 96.65%), 원문 조사일과 겹치는 8월 13~17일로 제한하면 `0.247684`다. 후자는 조사 8일 전체나 사용계획을 담지 않으므로 유리한 사후 창으로 선택하지 않는다. 원장 윈도만 달리해도 결과가 크게 달라지는 증거다.

로컬 코드에서 최종 정책결제 검증 후 같은 거래의 결제액·신규 응답을 재결합했다. 거래가 줄거나 응답이 없으면 MPC를 채우지 않고 하한·상한 및 미분류 결제액을 남기며, 표준 채점기는 전액 귀속되지 않은 비율에 등록 적중을 주지 않는다. 자세한 원문·원장 비교와 다음 P010 검증 조건은 `docs/P010_EMPIRICAL_ALIGNMENT_20260926.md`에 있다. 현재 A100 P012 실행 코드는 건드리지 않았다.

## 9월 26일 — P016 원문·즉시 할인 기전 감사

P016 C1 원문의 +6.957%는 **5개 대형유통업체의 농축산물 상품별 2020년 판매액을 2018~2019년과 비교한 DID**다. 기존 시뮬 지표는 서울 대상 POI의 모든 상품 매출을 시행 전후 이틀로 비교하므로 직접 크기 차감할 수 없다. C2/3의 +11.6%·+4.6%·+1.9%p도 대형마트 내부 상품 구성의 사업기간 기술통계라 매장 유형 비중과 다르다. 원문·출력 계보는 `docs/P016_EMPIRICAL_ALIGNMENT_20260926.md`에 기록했다.

더 근본적으로 `sector_voucher`는 할인 사실만 프롬프트에 보여 주고 시민 결제·잔액에는 할인율과 한도를 적용하지 않았다. 로컬 코드에 정책 JSON 기반의 즉시 정률 할인 회계를 추가해 판매자 총매출과 시민 순부담, 거래별 할인 영수증, 누적 한도, 월별 자기부담 지출을 분리했다. 아직 **실행 중인 서버에는 배포하지 않았다**. 다음 P016 런 전 관문은 실제 적격 매장만 할인·한도 소진 후 0원·잔액 순부담·원장 일치와 월간 결과 보존이다. 상품 단위 외부 검증 자료가 없어 POI 대리 지표는 기전·방향 참고치에 한정한다.
