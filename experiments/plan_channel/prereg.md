# 사전등록 — 계획의 정책 반응을 총액에 통과시키면 오차가 주는가

**2026-09-24 07:30** · 본런은 아직 안 돌았다 · 근거 `../error_budget/diagnosis_05.md`

---

## 무엇을 바꾸나 — 회계 한 줄. **프롬프트는 v5 그대로.**

```
현재   total = max(anchor, planned)
고침   total = anchor x clamp(planned / 그 사람의 기준 계획액, 0.5, 2.0) x 0.88144
```

기준선은 **그 런의 워밍업 3일(10-18·19·20)** 에서 잰 개인별 평균 계획액이다.
**두 비교창(10-21·22 / 10-25·26) 밖**이라 비교를 오염시키지 않는다.

클램프와 SCALE 은 **정답지를 안 보고** 정했다 — 앵커와의 순위상관을 현행의
90% 이상 지키는 가장 넓은 구간이 [0.5, 2.0] 이고(92% 보존), SCALE 은 무정책
창의 총액 평균이 현행과 같아지게 하는 값이다.

## 왜 — 정책은 이미 닿고 있었다

라운드2 런(r2_v5)의 같은 에이전트 쌍을 맞대면

```
Stage2 계획액   69,839 -> 76,652   **+9.76%**  286:214  p=0.0015
소비성향 스칼라    0.70 ->   0.70       +0.34%  동점 140  p=0.712
총액            130,418 -> 133,512     +2.37%  동점 43   p=0.640
```

계획이 앵커를 이기는 경우가 **23.7%** 뿐이라 반응의 4분의 3 이 먹힌다
(0.237 x 9.76% = 2.31% ~ 관측 +2.37%). **기전이 산술로 닫힌다.**

## 미리 적는 예측

```
1차  P012-1 이 현행에서 **커진다.**
     같은 자료 재계산으로는 +2.37% -> +5.41%, 오차 18.45 -> **15.41%p** (-3.04%p)
     런에서 그만큼 나오리라는 보장은 없다 — 계획 자체가 다시 뽑히기 때문이다
2차  **총 오차가 줄어야 성공이다.** 이 고침은 모든 에이전트의 하루 총액을
     바꾸므로 EM-3·DS-1·DS-2 도 함께 움직인다. P012-1 만 좋아지고 나머지가
     그만큼 나빠지면 **기각한다**
3차  P012-2(새 눈금 n=500)·P012-5 값이 처음으로 나온다
```

## 무엇이 이 실험을 죽이나

```
· 관문에서 총액이 하나도 안 달라지면 -> 경로가 안 걸린 것. 본런 안 돌린다
· 관문에서 수준이 25% 넘게 흔들리면  -> 폭주. 본런 안 돌린다
· 총 오차가 안 줄면                  -> 기각. 회계를 되돌린다
· P012-1 이 오히려 **실측을 넘어서면**(+20.82% 초과) 과교정이다.
  클램프를 좁히는 것이 아니라 **왜 넘쳤는지**를 먼저 적는다 —
  정답지에 맞춰 클램프를 조이면 그때부터는 맞춘 것이 아니라 끼워 맞춘 것이다
```

## 순환이 아니다

바꾸는 것은 **회계**고 재는 것은 원장이다. 계획액은 에이전트 자신의 이벤트
산물이지 측정 공식이 아니다. 기준선은 정책 **전** 날짜에서만 잰다. 프롬프트는
한 글자도 안 바꾼다 — 그래서 이 런의 차이는 온전히 회계의 것이다.

## 이 다음이 프롬프트 최적화다

이 고침이 서면 **프롬프트 -> 이벤트 -> 계획액 -> 총액 -> 지표** 통로가 이어진다.
이벤트 채널은 프롬프트에 반응한다는 것이 두 번 측정됐다(문턱 격자에서 적립
이벤트 3.75 -> 4.67, 24% 변동 · 정책 반응 +9.76%). 스칼라 채널은 죽었지만
(`../split_anchor/calib_02.md`) **이 통로는 살아 있다.** 통로를 이어 놓고
프롬프트를 손대는 것이 순서다.

---

# 부록 — 기대 효과의 신뢰구간 (본런 **전**, GPU 0)

**2026-09-24 09:20** · 라운드2 런의 계획 자료로 재계산 · 에이전트 500 · 부트스트랩 400회

```
규칙                    효과       95% 신뢰구간         P012-1 오차
현행 max(앵커,계획)     +2.37%   [-1.11, +6.86]       18.45%p   ← **0 을 지난다**
고침 개인 기준선        +5.41%   [+1.90, +9.36]       15.41%p   ← 0 을 안 지난다
```

## 이것이 말하는 것

고침은 P012-1 을 **"0 과 구별 안 됨" 에서 "유의하게 양수" 로** 옮긴다. 부호
적중이 우연이 아니게 되는 것이 이 고침의 1차 효과다.

## 이것이 말하지 **않는** 것

개선폭(-3.04%p)은 신뢰구간 폭(약 7.5%p)보다 **작다.** 그러므로 한 번의 런에서
"오차가 3%p 줄었다" 를 통계적으로 주장할 수 없다. 재계산은 정밀하지만(LLM 잡음
없음) 런은 아니다. **판정문에 이 한계를 함께 적는다.**

```
주장할 수 있는 것   고침 쪽 구간이 0 을 안 지난다
주장할 수 없는 것   고침이 현행보다 3.04%p 낫다 (구간이 겹친다)
```

---

# 관문 결과 (2026-09-24 14:39) — 통과

```
계획            끄고 100 · 켜고 100
총액이 달라진 사람  79 / 100 (79.0%)          <- 경로가 살아 있다
총액 중앙        97,349 -> 101,204 (+3.96%)  <- 폭주 없음 (한계 25%)
```

**첫 시도는 실패했다** — 계획 0건. 원인은 정책도 회계도 아니고 내 패처였다.
`from . import v5offsite` 를 심으면서 **파일은 안 옮겨** 거시 저장소의 prompts
가 ImportError 로 죽었다. 모든 런이 import 단계에서 죽는 상태였다.

**관문이 본런을 막아 준 것이 이 설계의 값어치다.** 안 막았으면 8시간을 날리고
원인을 나중에 찾았을 것이다. 패처의 그 단계는 비웠다(죽은 레버용이었다).

## +3.96% 를 어떻게 읽나

관문이 도는 날(2021-10-18)은 **워밍업 첫날**이고 개인 기준선도 10-18·19·20 의
평균이다. 그러니 첫날의 계획/기준선 비가 정확히 1 일 이유가 없다. SCALE 은
**무정책 창(10-21·22)** 에서 수준이 보존되도록 잡은 값이므로, 수준 보존은
거기서 확인해야 한다. 관문이 본 것은 **폭주가 아니라는 것**까지다.

본런 시작 14:39:50 · N=500 · 9일.

---

# 9일 런 중단 (2026-09-24 18:35) — 두 가지 이유

## 1. 창이 유인을 잘라내고 있었다

`../error_budget/diagnosis_06.md` — 9일 창에서는 정책 시작 시 문턱의 64% 에
머물고 **91% 가 관측 창 안에 문턱을 못 넘는다.** 이 창의 P012-1 은 회계를
고치든 안 고치든 정답지 근처에 못 간다. 회계 고침을 검증하는 값어치는 남지만,
**같은 GPU 를 28일 창에 쓰는 것이 낫다.**

회계 고침은 이미 세 겹으로 확인돼 있다.

```
· 결정적 재계산 + 부트스트랩 CI  +2.37% -> +5.41% · [+1.90, +9.36]
· LLM 없는 관문 둘             표본 겹침 100% · 분기가 다른 수를 냄
· 실제 런에서 적용 확인         2,800 / 2,800 행이 새 경로를 탔다
```

## 2. **런을 두 개 동시에 돌리고 있었다**

```
PID 1441768  --start 2021-10-18 --days 9 --limit 500   (14:39 시작)
PID 1442596  --start 2021-10-18 --days 9 --limit 500   (14:43 시작)
```

14:24 에 띄운 러너(옛 관문)가 관문을 통과해 **본런을 이미 시작한 상태**였는데,
관문을 고치고 14:43 에 **다시 띄웠다.** 앞 프로세스를 안 죽였다. 둘이 같은
출력 폴더와 같은 그래프에 함께 썼다.

**같은 실수를 오늘 두 번째로 했다** — 탐침에서도 두 프로세스가 같은
responses.jsonl.part 에 함께 썼다. 그때 런북에 적었는데 러너에는 적용하지
않았다.

```
규칙: 띄우기 전에 **같은 이름의 프로세스가 도는지 먼저 본다.**
      rm -rf 로 출력 폴더를 지우는 것은 앞 프로세스를 멈추지 않는다.
```

자료는 `/data/plan_p012_corrupt_두개동시` 로 옮겨 두었다. **읽지 않는다** —
두 런이 섞였으므로 무엇이 누구 것인지 가를 수 없다.

## 다음

후보 4 탐침이 GPU 를 온전히 쓰게 두고, 판정이 나면 살아남은 프롬프트로
**28일 창 런**(`tools/run_p012_28d.sh`)을 건다.
