# arm A 가 무엇을 내야 하는가 — **결과를 보기 전에 적는다**

**2026-09-24 02:40** · arm A 진행 중(3/12일) · 채점 파일 0건

---

## 왜 지금 적나

거리두기에서 프롬프트를 한 글자도 안 고치고 n 을 200 → 500 으로 올린 것만으로
DS-1·DS-4 의 구간이 0 을 벗어났다. **"표본이 병목" 이 참이면 P013 에서도 같은
일이 일어나야 한다.**

그 예측을 결과가 나온 뒤에 적으면 무엇을 적든 맞은 것처럼 보인다. 지금 적는다.

## 역산이 말하는 것

`power_to_detect_truth` 가 n=200 런에서 되돌린 필요 표본이다.

```
지표    n=200 에서의 필요 n    n=700 이면
EM-2          280             넘긴다 (2.5배 여유)
EM-3          620             아슬아슬하게 넘긴다 (1.1배)
```

## 등록하는 예측

```
① EM-3   구간이 0 을 벗어난다. 다만 여유가 1.1배라 **간신히**일 것이다.
         못 벗어나도 역산이 어림이었다는 뜻이지 프롬프트 탓이 아니다.

② EM-2   **이 라운드가 EM-2 를 처음 제대로 잰다.** 지금까지의 +13.1% 는
         적격 판정이 상생소비지원금 기준이었다(못 셈으로 표시해 둔 값).
         고친 자로 재면 **값이 달라질 것이다.** 어느 쪽으로 달라질지는
         예측하지 않는다 — 업종 집합이 바뀌므로 방향을 걸 근거가 없다.

③ EM-4   순위. 지금까지 '관측부족' 이었는데 그 이유가 둘이었다 —
         순위 대상 이름이 우리 업종에 없었고(2026-09-23 고침), n 이 작았다.
         **둘 다 고쳐졌으므로 이번엔 값이 나올 것이다.**

④ 부호 적중  P013 이 2/3 이상이면 전 정책 적중률이 57% 에서 오른다.
```

## 예측하지 않는 것

**크기.** EM-3 의 실측 +7.3% 와 시뮬 값의 차이를 정확도로 읽지 않는다.
원문은 전년 동기 대비고 우리는 같은 해 안의 전후다.

**arm B.** 런 간 이동은 arm B 가 있어야 나온다. arm A 만으로는 "이 값이
프롬프트의 성질인가" 에 답하지 못한다.

## 이 예측이 빗나가면

`SAMPLE_IS_THE_BINDING_CONSTRAINT` 와 `SELECTED_PROMPT` 의 핵심 주장이
약해진다. **그때는 그렇게 적는다** — 표본을 올렸는데도 구간이 0 을 지나면
병목이 표본만은 아니라는 뜻이다.

> 실행 중에 이 문서를 커밋한다. 채점 파일이 아직 0건이다.
