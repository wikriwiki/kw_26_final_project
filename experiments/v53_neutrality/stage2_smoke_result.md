# v53 Stage2 기술 화면 결과 (2026-09-26)

사전등록한 [`stage2_smoke_prereg.md`](stage2_smoke_prereg.md)와 설정대로, 동결된 무정책 Stage1 문맥 12개에 일반 출력·온도 0과 JSON Schema·온도 0의 일정 두 개씩을 붙여 **24개 Stage2 호출**을 완료했다. 네 사회 배경에서 각 6개다. 시민은 8명으로 반복되므로 24개를 독립 표본처럼 해석하지 않는다. 기존 A100 EXAONE-4.5-33B-AWQ, v53 중립 Stage2, 첫 시도 온도 0.7·2,200토큰 상한을 사용했다. 그래프에는 읽기 전용 POI 후보 조회만 했고 모든 후보를 호출 전에 동결했다. 정책 팔을 실행하거나 기존 그래프를 초기화하지 않았다.

| 기술 지표 | 전체 | Stage1 일반 출력·온도 0 | Stage1 Schema·온도 0 |
|---|---:|---:|---:|
| 호출/완료 | 24/24 | 12/12 | 12/12 |
| 첫 호출 성공 | 24/24 | 12/12 | 12/12 |
| 완전 실패 | 0 | 0 | 0 |
| 후보 밖·다른 order 장소 보정 | 0 | 0 | 0 |
| 누락 선택 자동 채움 | 0 | 0 | 0 |
| 금액 자동 대체 | 0 | 0 | 0 |
| 일정 이벤트 범위 | 9~13 | 9~13 | 9~10 |

원시 JSON 24개는 모두 `finish_reason=stop`이었다. 63개 장소 선택의 order와 POI가 각 이벤트의 동결 후보 풀에 정확히 속했고, `actual_spent`는 모두 양수였다. 무정책 입력에서 `policy_spend`가 채워지거나 `extra_spent`가 양수인 선택은 없었다. 출력 토큰은 중앙값 381.5, 최대 540으로 2,200토큰 상한에서 멀었다. 이 화면은 외부 방문이 일정당 1~4개라 실제 대규모 하루에서 발생한 1,400토큰 절단·리뷰 장애를 재현하지 않는다. 두 Stage1 출력 방식 사이에 일정 길이 외 다른 내용도 달라져 길이의 인과 효과를 추정할 수 없다.

이 결과는 **Stage2 기술 연동의 작은 양성 화면**이다. 과거 날짜의 Stage1 문맥에 현재 P012 완료 그래프의 후보 인지도·만족도가 붙었고, 정책은 모두 OFF다. 정책 행동, 효과의 방향·대략적 크기, 범용 최적 프롬프트 여부를 판정하지 않는다. 24/24 성공만으로 생산 95% 관문을 통과했다고 말하지 않는다. 더 큰, 실제 정책 ON/OFF 쌍체 실험 전에는 정합한 적격 규칙·원문 지표·그래프 복원 백업이 필요하다.

이번 정적 관문에서 범용 v53을 P010·P011·P013에 그대로 적용하면 사용처 적격 규칙과 표시 선언이 없어 **실패**함을 확인했다. 과거 프롬프트와 정책 원본을 묵시적 쿠폰 규칙으로 대체하지 않도록 막았다. P014·P015·P016에는 명시된 적격 규칙이 있으며 P012는 사용처 제한형이 아니다. 제한형 정책 둘을 동시에 적용하는 것은 현 후보 표현이 첫 정책의 규칙만 담으므로 중립 실행에서 차단한다. 다음 검증은 각 정책의 실측 사용처 범위를 표현하는 별도 실험용 정책 명세를 작성·감사한 뒤 진행해야 한다. P015 보관 홀드아웃은 재실행하지 않는다.

사전등록·코드 커밋은 `9ff4ac2`, 실행기 import 수정 커밋은 `5be799a`다. 원본 `inputs.json`, `manifest.json`, `responses.jsonl`, `results.jsonl`, `summary.json`은 `output/validation_v53_stage2_smoke_20260926`와 `C:\Users\Administrator\Documents\kw26_a100_recovery_20260926\validation_v53_stage2_smoke_20260926`에 각각 보존했고 파일마다 SHA256 일치를 확인했다. 매니페스트는 설정·실행기·프롬프트·Stage2 코드·앞선 Stage1 원본과 동결 입력 지문을 포함한다.

| 파일 | SHA256 |
|---|---|
| `inputs.json` | `8f2ae3bdd8db2cea245bf88c14aa14356dc9f3ab1f31cb42f80e178aac2bdb51` |
| `manifest.json` | `c851bb84734c0da6b1190236eeb17d9aa86642b1e8ffd9e7f9c2b0f05f9c2435` |
| `responses.jsonl` | `a72ec6b0bd1d2879c19f1644f4ff5385949a62cc11be5b3ecf0592b6cc533ff2` |
| `results.jsonl` | `56040f75b79348cddef9d73400c7a72eb602141f53be928cde19a8e4fa2c6d5c` |
| `summary.json` | `9ac9a1811123694c7a4a61961c6db6022f2f503956dce8bdb90ace81d84ad07d` |
