# 다음에 무엇을 하는가 — 명령까지 적는다

**2026-09-24 19:20 갱신**

---

## 먼저 읽을 것 — 오늘 판이 두 번 바뀌었다

```
error_budget/diagnosis_05.md   정책은 계획액으로 닿고 있었다 (+9.76%, p=0.0015)
                               그런데 max(앵커,계획)이 4분의 3을 버린다
error_budget/diagnosis_06.md   **9일 창에서 91%가 문턱에 못 닿았다**
                               후보 넷이 기각된 진짜 이유가 여기다
plan_channel/*_result.md       후보 1·2·3·4 전부 기각 (p=0.678/1.000/0.798/1.000)
```

**한 줄 요약**: 프롬프트가 못 한 게 아니라 **반응할 유인이 창 밖에 있었다.**

## 지금 돌고 있는 것

```
28일 런   /data/p012_28d   v5 · N=500 · 10-01~10-28 · 회계 고침 **꺼짐**
          1/28일 · 투영 문턱 대비 **0.96** (한계 구간 안) · 약 33시간
재탐침    /data/s1_when2   후보 4 를 마감 3일 + 문턱 0.93 에서 다시 묻는다
          240 호출 · 워커 4
```

## ① 재탐침이 끝나면

```bash
ssh -i outofmemory.pem -p 10022 outofmemory@123.37.28.167 \
  'sed -n "/판정/,$p" /data/s1_when2/probe.log | tail -14'
```

```
내구재가 유의하면   후보 4 가 산다. 28일 런에 올릴 프롬프트가 생긴다
기각되면           **후보 4 는 끝이다** — 조건을 또 바꾸지 않는다고 등록했다
                   그때 결론: 마감과 한계가 둘 다 있어도 프롬프트가 행동을
                   못 바꾼다. 남는 지렛대는 회계와 창뿐
```

## ② 28일 런이 끝나면 (약 33시간)

러너가 채점·문턱 모양 진단·다음 런용 기준선 생성까지 자동으로 한다.

```
score_28d_v5.json          P012 지표
threshold_response_shape   **반응이 문턱 언저리에 몰렸는가** — 유인이 살았다는 증거
/data/plan_baseline_28d.json  다음 런(회계 고침)용 개인 기준선
```

읽는 순서: **문턱 모양 먼저.** 언저리에 몰렸으면 기전이 처음으로 작동한 것이고,
그다음에야 P012-1 의 크기를 읽는 의미가 있다.

## ③ 그다음 — 회계를 더한다

```bash
# 같은 창 · 같은 프롬프트 · EXP_PLAN_DRIVES_TOTAL=1 + ②가 만든 기준선
# run_p012_28d.sh 에서 unset/0 두 줄만 되돌리면 된다
```

창과 회계를 **한 번에 바꾸지 않는다** — ②가 창의 효과, ③이 회계의 효과다.

## 중간 점검 (14일쯤)

```bash
# 투영이 0.8~1.1 밖으로 나가면 창을 다시 본다
python scripts/report/threshold_response_shape.py <metrics> --off ... --on ...
```

## 끝난 것

```
프롬프트 후보 1  문턱 -> Stage2 금액      p=0.678   Stage2 는 업종 단가에 묶여 있다
프롬프트 후보 2  빈자리 줄 -> Stage1      p=1.000   (1차 6:1 은 실질표본 7의 잡음)
프롬프트 후보 3  trigger -> Stage2 금액   p=0.798   대조군도 안 움직임(눈은 멀쩡)
프롬프트 후보 4  시점 채널 -> Stage1      p=1.000   재시험 중 (마감 3일 조건)
회계 고침        EXP_PLAN_DRIVES_TOTAL    심음 · 시험 15개 · 재계산 -3.0%p · CI 0 안 지남
눈금 이동        P012-2 -> st.online_spent  라이브 700쌍 확인
28일 창          설계·시드 버그 수정·0.93 확인 완료
```

## 표적 — 크기 순 (2026-09-24 기준)

```
1  EM-4 과반응 61.10%p   가장 크다. 진단은 했고(예시 3:1 비대칭) **교란 확인이 막혔다**
                        -> 다음 P013 런에서 무정책 창 + 업종군 per-agent 를 함께 남긴다
2  P012-1 18.92%p       쌍 바닥 17.97 (제외 채널이 자료에 없다). 창은 P012-3/4/6 을 채운다
3  DS-1 4.40%p          case_trend 후보 복제 기각
```

## 함정 — 오늘 당한 것들

```
· **띄우기 전에 같은 이름의 프로세스가 도는지 본다.** rm -rf 는 앞 것을 안 멈춘다
  (오늘 두 번: 탐침 둘이 같은 .part, 본런 둘이 같은 폴더·그래프)
· scp 한 셸 스크립트는 반드시 sed -i "s/\r$//" — CRLF 면 export 가 조용히 깨진다
· 관문이 "달라졌나" 를 물으면 관문이 아니다. **"내가 켠 그것 때문에?" 를 묻는다**
  (limit 100 표본이 기준선과 2명만 겹친 채 "79% 달라짐 — 통과" 를 냈다)
· 동점률이 높으면 쌍 수가 아니라 **불일치 쌍 수**가 표본이다
· 집계 필드는 이름이 아니라 정의를 본다 (cm_plan_over_anchor 가 0.5% vs 실제 23.7%)
· 정책 파일의 notes 를 먼저 읽는다 — 28일이 필요하다고 이미 적혀 있었다
· ps/pgrep 패턴이 **자기 명령줄에 걸린다**. awk 로 자기 자신을 빼고 센다
· **긴 런 앞에서는 기전이 작동할 범위에 있는지를 LLM 없이 먼저 센다**
```
