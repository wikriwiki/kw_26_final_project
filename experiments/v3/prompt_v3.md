# 실험 v3 — 제도를 어떻게 읽게 할 것인가 (후보 비교)

**날짜** 2026-09-20 · **상태** 완료 — **v26 기각, v25 유지**

---

## 이번에 바뀐 점

**드디어 프롬프트를 바꾼다.** v1(게이트)·v2(완전 실행)는 측정 경로를 고치는 일이었고,
이번이 프롬프트 후보를 겨루는 첫 실험이다.

바꾸는 것은 **딱 한 축**이다 — 프롬프트가 제도를 어떻게 다루게 할 것인가.
현재 v25는 제도를 여섯 입력 중 하나로 한 절에 담고 있다.

```
그 사람의 생활·선호, 확정된 의무, 현재 필요·재고·자금, 장소와 상품, 제도와 사회 배경을 함께 고려한다.
```

## 눈여겨 볼 점

**① 한 축만 바꾼다.** 세 후보는 제도 관련 문장을 빼면 **글자 단위로 같다.**
검사로 강제했다(`test_only_the_institution_axis_differs`). 차이가 나면 그 축 때문이다.

**② 방향을 가리키지 않는다.** v26이 더하는 세 갈래는 전부 **"달라지기도 한다"** 로
적었다. 늘어난다고도 줄어든다고도 말하지 않는다. 검사로 강제했다
(`test_the_three_directions_are_written_symmetrically`).

**③ 하한 대조군이 있다.** v27은 제도 문장을 **아예 뺀다.** 제도 사실은 입력에 그대로
있고, 프롬프트가 그것을 언급하지 않을 뿐이다. **v26이 v27을 이기지 못하면 더한
문장은 아무 일도 하지 않은 것이다.** 1차에서 v6이 했던 역할이다.

**④ 이번엔 무엇으로 이기나.** 실측 정답과 맞는지로 고르지 **않는다.** 그건 추정량
정렬이 끝난 뒤의 별도 단계다(직전 인수인계 6번). 이번 판정은 **실행 품질**로 한다 —
정답을 몰라도 답할 수 있는 것들이다.

---

## 후보 — 집합 마감. 더 추가하지 않는다

| 후보 | 제도 취급 |
|---|---|
| **v25** | 현직·대조군. 여섯 입력 중 하나로 한 절 |
| **v26** | v25 + 2단 읽기 — *"오늘 무엇이 달라졌는지 보고, 그것이 이 사람이 하려던 일에 닿는지 판단한다. 닿지 않으면 평소와 같다."* |
| **v27** | **하한 대조군** — 제도 절을 뺀다 |

v26이 더하는 전문:

```
제도와 사회 배경은 오늘의 조건을 바꾼다. 쓸 수 있는 돈이 달라지기도 하고,
같은 돈으로 살 수 있는 양이 달라지기도 하고, 할 수 있는 일이 달라지기도 한다.
먼저 오늘 무엇이 달라졌는지 보고, 그것이 이 사람이 하려던 일에 닿는지 판단한다.
닿지 않으면 평소와 같다.
```

> 정책 이름·원하는 부호·실측 목표·수치가 하나도 없다. 검사로 막아 두었다.

## 판정 기준 — 돌리기 전에 정한다

**1차 (실행 품질, 정답 불필요)** — 순서대로 본다.

| | 기준 |
|---|---|
| 계약 통과 | 계획 96/96, 요청 실패 0, 사후 보정 0 |
| 물리 정합 | **자원 게이트 제외 수.** 적을수록 낫다 |
| 결제 정합 | 회계·재고·배송 독립 검사 통과 행 수 |
| 미충족 처리 | 필요를 못 채운 경우를 지출 할당량으로 착각하지 않는가 |

**2차 (기전 분별력)** — 세 후보가 **완전 행렬**을 냈을 때만 본다.
네 기전의 on/off 차이가 후보마다 **다르게 나오는가**. 어느 방향이 옳은지는 여기서
묻지 않는다. 제도 문장이 아무 일도 안 한다면 v26과 v27의 대조가 서로 구별되지 않을 것이다.

**하면 안 되는 것** — 실측 부호와 맞는 후보를 고르기. 그것이 순환 검증이고,
이 프로젝트가 v5에서 무너진 이유다.

## 2차 판정의 잣대 — v3 결과를 보기 전에 정한다

v2에서 같은 프롬프트가 seed만 바꿔도 기전 부호가 뒤집히는 것을 봤다. 그래서 **v25 한
프롬프트의 seed 변동 폭**을 먼저 쟀다. 이것이 잡음 바닥이다.

| 기전 | seed 수 | 최소 | 최대 | **폭** |
|---|---:|---:|---:|---:|
| 캐시백 | 3 | +167 | +2,833 | 2,667 |
| 거리두기 | 2 | −1,917 | +1,000 | 2,917 |
| 지원금 | 3 | +833 | +3,000 | 2,167 |
| 지역화폐 | 3 | −583 | +3,417 | 4,000 |

**평균 폭 2,938원 · 최대 4,000원** (1인 1일). 기록은
`data/experiments/seed_variation_band_v25.json`.

### 이것이 뜻하는 것

기전 효과 자체가 ±3,000원 범위인데 **같은 프롬프트의 seed 잡음이 그와 같은 크기다.**
후보 하나당 seed 하나로는 **후보 간 차이를 기전 대조로 읽을 수 없다.**

그래서 못 박는다.

- **2차 판정은 후보를 고르는 데 쓰지 않는다.** 차이가 위 폭 안이면 "구별 불가"로 적는다
- 폭을 넘는 차이가 나와도 **seed 하나로는 후보 탓이라고 말하지 않는다.** 그때는
  반복 seed를 별도 단위로 두는 설계를 v4로 넘긴다
- **후보 선택은 1차 판정(실행 품질)으로만 한다.** 계약 통과, 게이트 제외 수,
  회계·물리 검사, 미충족 처리 — 정답을 몰라도 답할 수 있고 seed 잡음에 덜 흔들린다

거리두기의 폭은 seed 2개로만 잰 것이라 **과소평가일 수 있다**(57001은 채점 거부).

## 실행 조건 — v2가 먼저 자격을 얻어야 한다

v2가 자격 있는 완전 행렬을 냈다 — 계획 96/96, 게이트 제외 0, 결제 96/96.
조건을 채웠으므로 실행한다.

설계는 v2와 같다 — 같은 12명 × 4기전 × on/off = 96칸, 게이트 활성.
후보 셋이므로 계획 288칸이다. seed는 v2 결과를 보고 정하지 않고 **59001로 고정**한다.

## 결과 — v26 기각. v25 가 남는다

수치는 [graph_v3.md](graph_v3.md).

### 1차 (실행 품질) — 셋이 똑같다. 못 가른다

| | v25 | v26 | v27 |
|---|---|---|---|
| 계획 | 96/96, 보정 0 | 96/96, 보정 0 | 96/96, 보정 0 |
| 게이트 제외 | 0 | 0 | 0 |
| 결제 | 96/96 `all_pass` | 96/96 `all_pass` | 96/96 `all_pass` |

**사전등록한 1차 판정으로는 후보를 고를 수 없다.** 셋 다 완벽하게 통과했다.
이것도 결과다 — 제도 문장을 어떻게 쓰든 계약·물리·회계는 흔들리지 않는다.

### 2차 (기전 분별력) — 예상대로 잣대 아래다

| 기전 | 후보 간 폭 | 잡음 폭 | 판정 |
|---|---:|---:|---|
| 거리두기 | 3,333 | 2,917 | 폭 초과 |
| 지역화폐 | 2,750 | 4,000 | 구별 불가 |
| 지원금 | 1,500 | 2,167 | 구별 불가 |
| 캐시백 | 2,833 | 2,667 | 폭 초과 |

폭을 넘은 둘도 **후보당 seed 하나이므로 후보 탓이라고 말하지 않는다** — 사전등록한 대로다.
캐시백에서 v25 +1,417 · v26 −1,417 로 부호가 갈리는 것이 그 이유를 보여 준다.

### 반응성 (사전등록) — v26 실패, 그런데 v27 이 말해 주는 것이 있다

영점(그냥 다시 뽑으면 달라지는 정도) 대비 초과분:

```
              동선      구매칸
v25         +0.021      +0.141
v26         +0.018      +0.094
v27         +0.001      +0.056     ← 제도 문장을 뺀 하한
```

- **v26 은 사전등록 기준으로 실패다.** 동선 초과분 +0.018, 성공선 +0.047 에 한참 못 미치고
  실패 구간(±0.034) 안이다. 구매칸에서도 v25 보다 낮다
- **v27 은 정확히 영점이다**(+0.001). 제도 문장을 빼면 계획이 **다시 뽑은 것과
  구별되지 않는다.** v25 의 한 절이 일을 하고 있다는 뜻이다
- 다만 v25(+0.021) 와 v27(+0.001) 의 차이 0.020 은 v25 의 seed 변동 SD 0.017 의
  1.2배다. **방향은 셋 다 같지만 결정적이지 않다**

### 그래서 무엇을 배웠나

**제도를 한 절로 넣는 것은 일을 한다. 그 위에 설명을 더 붙이는 것은 일을 하지 않는다.**

v25(한 절) > v26(한 절 + 2단 읽기 설명) > v27(없음) 이 두 지표 모두에서 같은 순서다.
산문을 늘린 v26 이 오히려 낮다. 이전 세대에서 얻은 것과 같은 결론이다 —
**서술은 안 먹고 계산된 사실이 먹는다.**

### 그런데 v3 이 답하지 못한 더 큰 것

v3 을 판정하는 과정에서 처치 자체를 들여다봤고, 거기서 이 실험이 왜
수렴하지 못했는지가 나왔다 — [TREATMENT_BINDS.md](../TREATMENT_BINDS.md).

계획의 앵커 866개 중 `zone:*` 이 93개뿐이고, 제한 지갑이 통하는 자리를 계획에 넣은
칸이 **12칸 중 0~2칸**이다. 그래서 결제 시점에 `eligible_wallets` 가 비어 오고
정책 자금 사용이 지갑의 0.3%에 그친다. **프롬프트가 닿을 대상이 거의 없었다.**

거리두기는 더하다. 24칸의 활동에 카페·심야외식이 **0건**이라 잴 것 자체가 없었다.
v26 의 "할 수 있는 일이 달라지기도 한다"가 동선을 못 바꾼 것도 이 때문이다.

**v4 는 그 자리를 연다** — [v4/prompt_v4.md](../v4/prompt_v4.md).
